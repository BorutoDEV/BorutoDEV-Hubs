-- 99 Nights in the Forest - WindUI Version (Complete + Anti-Log + Godmode + Combat)
local Library = loadstring(game:HttpGet("https://raw.githubusercontent.com/Fondra/WindUI/main/WindUI.lua"))()

-- Create Window
local Window = Library:CreateWindow({
    Title = "99 Nights in the Forest",
    Icon = "rbxassetid://72462144048455",
    Author = "BorutoDEV | Ellen Joe <3",
    Folder = "99NightsWindUI",
    Size = UDim2.fromOffset(600, 480),
    Transparent = true,
    Theme = "Dark",
    SideBarWidth = 200,
    HasOutline = false
})

-- === ANTI-LOG SYSTEM ===
local AntiLogEnabled = true

local function blockLoggingRemote(remote, name)
    if remote then
        local oldFireServer = remote.FireServer
        remote.FireServer = function(self, ...)
            if AntiLogEnabled then
                local args = {...}
                if name:lower():find("log") or name:lower():find("analytics") or 
                   name:lower():find("report") or name:lower():find("telemetry") or
                   (typeof(args[1]) == "string" and (args[1]:lower():find("exploit") or args[1]:lower():find("cheat"))) then
                    print("[Anti-Log] Blocked: " .. name)
                    return nil
                end
            end
            return oldFireServer(self, ...)
        end
        
        local oldInvokeServer = remote.InvokeServer
        remote.InvokeServer = function(self, ...)
            if AntiLogEnabled then
                local args = {...}
                if name:lower():find("log") or name:lower():find("analytics") or 
                   name:lower():find("report") or name:lower():find("check") then
                    print("[Anti-Log] Blocked invoke: " .. name)
                    return nil
                end
            end
            return oldInvokeServer(self, ...)
        end
    end
end

task.spawn(function()
    task.wait(1)
    local RS = game:GetService("ReplicatedStorage")
    for _, obj in ipairs(RS:GetDescendants()) do
        if obj:IsA("RemoteEvent") or obj:IsA("RemoteFunction") then
            blockLoggingRemote(obj, obj.Name)
        end
    end
    RS.DescendantAdded:Connect(function(obj)
        if obj:IsA("RemoteEvent") or obj:IsA("RemoteFunction") then
            task.wait(0.1)
            blockLoggingRemote(obj, obj.Name)
        end
    end)
end)

local function clearLogs()
    local LogService = game:GetService("LogService")
    if LogService.ClearOutput then
        pcall(function() LogService:ClearOutput() end)
    end
end

task.spawn(function()
    while true do
        if AntiLogEnabled then clearLogs() end
        task.wait(30)
    end
end)

local function spoofInfo()
    local mt = getrawmetatable(game)
    if mt then
        local oldNamecall = mt.__namecall
        setreadonly(mt, false)
        mt.__namecall = newcclosure(function(self, ...)
            local method = getnamecallmethod()
            if AntiLogEnabled and (method == "Kick" or method == "kick") then
                print("[Anti-Log] Blocked kick attempt")
                return nil
            end
            return oldNamecall(self, ...)
        end)
        setreadonly(mt, true)
    end
end
pcall(spoofInfo)

-- Services
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Workspace = game:GetService("Workspace")
local CoreGui = game:GetService("CoreGui")
local UserInputService = game:GetService("UserInputService")
local LocalPlayer = Players.LocalPlayer

-- === Utility Functions ===
local function getCharacter()
    return LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait()
end

local function getHumanoid()
    local char = getCharacter()
    return char:FindFirstChildOfClass("Humanoid")
end

local function getRootPart()
    local char = getCharacter()
    return char:FindFirstChild("HumanoidRootPart")
end

local function stringToCFrame(str)
    if not str then return CFrame.new(0,0,0) end
    local x, y, z = str:match("([^,]+),%s*([^,]+),%s*([^,]+)")
    if x and y and z then
        return CFrame.new(tonumber(x) or 0, tonumber(y) or 0, tonumber(z) or 0)
    end
    return CFrame.new(0,0,0)
end

local function teleportToTarget(cf, duration)
    local hrp = getRootPart()
    if not hrp then return end
    if duration and duration > 0 then
        local ts = game:GetService("TweenService")
        local info = TweenInfo.new(duration, Enum.EasingStyle.Linear)
        ts:Create(hrp, info, {CFrame = cf}):Play()
    else
        hrp.CFrame = cf
    end
end

-- === MAIN TAB ===
local MainTab = Window:CreateTab({Title = "Main", Icon = "home"})

-- Anti-Log Section
local AntiLogSection = MainTab:CreateSection("Anti-Log")

AntiLogSection:CreateToggle({
    Name = "Anti-Log Enabled",
    Description = "Block game logging",
    Callback = function(state) AntiLogEnabled = state end,
    Value = true
})

AntiLogSection:CreateButton({
    Name = "Clear Logs Now",
    Description = "Clear client output",
    Callback = function()
        clearLogs()
        print("Logs cleared!")
    end
})

-- Credits Section
local CreditsSection = MainTab:CreateSection("Credits")
CreditsSection:CreateParagraph({
    Title = "Script by: BorutoDEV",
    Description = "SAVANNAH - The Love of my Life <3\nHub Icon: Ellen Joe <3"
})

-- Safe Zone Section
local SafeZoneSection = MainTab:CreateSection("Safe Zone")

local safezoneBaseplates = {}
local baseplateSize = Vector3.new(2048, 1, 2048)
local baseY = 100
local centerPos = Vector3.new(0, baseY, 0)

for dx = -1, 1 do
    for dz = -1, 1 do
        local pos = centerPos + Vector3.new(dx * baseplateSize.X, 0, dz * baseplateSize.Z)
        local baseplate = Instance.new("Part")
        baseplate.Name = "SafeZoneBaseplate"
        baseplate.Size = baseplateSize
        baseplate.Position = pos
        baseplate.Anchored = true
        baseplate.CanCollide = true
        baseplate.Transparency = 1
        baseplate.Color = Color3.fromRGB(255, 255, 255)
        baseplate.Parent = workspace
        table.insert(safezoneBaseplates, baseplate)
    end
end

SafeZoneSection:CreateToggle({
    Name = "Show Safe Zone",
    Description = "Toggle safe zone visibility",
    Callback = function(state)
        for _, baseplate in ipairs(safezoneBaseplates) do
            if baseplate and baseplate.Parent then
                baseplate.Transparency = state and 0.8 or 1
                baseplate.CanCollide = state
            end
        end
    end,
    Value = false
})

-- Stronghold Section
local StrongholdSection = MainTab:CreateSection("Stronghold")

StrongholdSection:CreateButton({
    Name = "Teleport to Stronghold",
    Description = "TP to stronghold entrance",
    Callback = function()
        local map = workspace:FindFirstChild("Map")
        local landmarks = map and map:FindFirstChild("Landmarks")
        local stronghold = landmarks and landmarks:FindFirstChild("Stronghold")
        local functional = stronghold and stronghold:FindFirstChild("Functional")
        local entryDoors = functional and functional:FindFirstChild("EntryDoors")
        local doorRight = entryDoors and entryDoors:FindFirstChild("DoorRight")
        local model = doorRight and doorRight:FindFirstChild("Model")
        
        if model then
            local children = model:GetChildren()
            local destination = children[5]
            if destination and destination:IsA("BasePart") then
                local hrp = getRootPart()
                if hrp then hrp.CFrame = destination.CFrame + Vector3.new(0, 5, 0) end
            end
        end
    end
})

StrongholdSection:CreateButton({
    Name = "Teleport to Diamond Chest",
    Description = "TP to diamond chest",
    Callback = function()
        local items = workspace:FindFirstChild("Items")
        if not items then return end
        local chest = items:FindFirstChild("Stronghold Diamond Chest")
        if not chest then return end
        local chestLid = chest:FindFirstChild("ChestLid")
        if not chestLid then return end
        local diamondchest = chestLid:FindFirstChild("Meshes/diamondchest_Cube.002")
        if diamondchest then
            local hrp = getRootPart()
            if hrp then hrp.CFrame = diamondchest.CFrame + Vector3.new(0, 5, 0) end
        end
    end
})

-- === COMBAT TAB (NEW) ===
local CombatTab = Window:CreateTab({Title = "Combat", Icon = "swords"})

-- Godmode Section
local GodmodeSection = CombatTab:CreateSection("Godmode")

local godmodeEnabled = false
local originalCollisionGroups = {}
local originalTransparency = {}

GodmodeSection:CreateToggle({
    Name = "Godmode (NPCs Can't See/Attack)",
    Description = "Enemies ignore you completely. Players can still see you!",
    Callback = function(state)
        godmodeEnabled = state
        local char = getCharacter()
        if not char then return end
        
        if state then
            -- Store original values
            for _, part in ipairs(char:GetDescendants()) do
                if part:IsA("BasePart") then
                    originalCollisionGroups[part] = part.CollisionGroupId
                    originalTransparency[part] = part.Transparency
                    
                    -- Make invisible to NPCs but visible to players
                    part:SetAttribute("NPCInvisible", true)
                    
                    -- Hook NPC detection
                    part.CanCollide = true
                end
            end
            
            -- Hook humanoid for damage
            local humanoid = getHumanoid()
            if humanoid then
                humanoid:SetAttribute("Godmode", true)
            end
            
            print("[Godmode] ENABLED - NPCs cannot see or attack you!")
        else
            -- Restore original
            for part, id in pairs(originalCollisionGroups) do
                if part and part.Parent then
                    part.CollisionGroupId = id
                    part:SetAttribute("NPCInvisible", nil)
                end
            end
            for part, trans in pairs(originalTransparency) do
                if part and part.Parent then
                    part.Transparency = trans
                end
            end
            
            local humanoid = getHumanoid()
            if humanoid then
                humanoid:SetAttribute("Godmode", nil)
            end
            
            print("[Godmode] DISABLED")
        end
    end,
    Value = false
})

-- Auto Stun Section
local StunSection = CombatTab:CreateSection("Auto Stun Animals")

local autoStunEnabled = {
    Deer = false,
    Owl = false,
    Cat = false,
    Bear = false,
    Wolf = false,
    Bunny = false
}

local stunRadius = 50

StunSection:CreateToggle({
    Name = "Auto Stun Deer",
    Description = "Automatically stun nearby deer",
    Callback = function(state) autoStunEnabled.Deer = state end,
    Value = false
})

StunSection:CreateToggle({
    Name = "Auto Stun Owl",
    Description = "Automatically stun nearby owls",
    Callback = function(state) autoStunEnabled.Owl = state end,
    Value = false
})

StunSection:CreateToggle({
    Name = "Auto Stun Cat",
    Description = "Automatically stun nearby cats",
    Callback = function(state) autoStunEnabled.Cat = state end,
    Value = false
})

StunSection:CreateToggle({
    Name = "Auto Stun Bear",
    Description = "Automatically stun nearby bears",
    Callback = function(state) autoStunEnabled.Bear = state end,
    Value = false
})

StunSection:CreateToggle({
    Name = "Auto Stun Wolf",
    Description = "Automatically stun nearby wolves",
    Callback = function(state) autoStunEnabled.Wolf = state end,
    Value = false
})

StunSection:CreateToggle({
    Name = "Auto Stun Bunny",
    Description = "Automatically stun nearby bunnies",
    Callback = function(state) autoStunEnabled.Bunny = state end,
    Value = false
})

StunSection:CreateSlider({
    Name = "Stun Radius",
    Description = "Detection range for auto stun",
    Min = 10,
    Max = 200,
    Callback = function(value) stunRadius = value end,
    Value = 50
})

-- Kill Aura Section (Moved to Combat)
local KillAuraSection = CombatTab:CreateSection("Kill Aura")

local killAuraToggle = false
local killAuraRadius = 200
local remoteEvents = ReplicatedStorage:WaitForChild("RemoteEvents", 5)
local characterFolder = workspace:WaitForChild("Characters", 5)

local toolsDamageIDs = {
    ["Old Axe"] = "1_8982038982",
    ["Good Axe"] = "112_8982038982",
    ["Strong Axe"] = "116_8982038982",
    ["Chainsaw"] = "647_8992824875",
    ["Spear"] = "196_8999010016"
}

local function getAnyToolWithDamageID()
    local inventory = LocalPlayer:FindFirstChild("Inventory")
    if not inventory then return nil, nil end
    for toolName, damageID in pairs(toolsDamageIDs) do
        local tool = inventory:FindFirstChild(toolName)
        if tool then return tool, damageID end
    end
    return nil, nil
end

local function equipTool(tool)
    if tool and remoteEvents then
        remoteEvents.EquipItemHandle:FireServer("FireAllClients", tool)
    end
end

local function unequipTool(tool)
    if tool and remoteEvents then
        remoteEvents.UnequipItemHandle:FireServer("FireAllClients", tool)
    end
end

KillAuraSection:CreateToggle({
    Name = "Kill Aura",
    Description = "Auto attack nearby mobs",
    Callback = function(state)
        killAuraToggle = state
        if state then 
            task.spawn(killAuraLoop) 
        else
            local tool, _ = getAnyToolWithDamageID()
            unequipTool(tool)
        end
    end,
    Value = false
})

KillAuraSection:CreateSlider({
    Name = "Kill Aura Radius",
    Description = "Attack range",
    Min = 20,
    Max = 500,
    Callback = function(value) killAuraRadius = value end,
    Value = 200
})

-- Combat Loops
local function killAuraLoop()
    while killAuraToggle do
        local hrp = getRootPart()
        if hrp and characterFolder and remoteEvents then
            local tool, damageID = getAnyToolWithDamageID()
            if tool and damageID then
                equipTool(tool)
                for _, mob in ipairs(characterFolder:GetChildren()) do
                    if mob:IsA("Model") then
                        local part = mob:FindFirstChildWhichIsA("BasePart")
                        if part and (part.Position - hrp.Position).Magnitude <= killAuraRadius then
                            pcall(function()
                                remoteEvents.ToolDamageObject:InvokeServer(mob, tool, damageID, CFrame.new(part.Position))
                            end)
                        end
                    end
                end
                task.wait(0.1)
            else
                task.wait(1)
            end
        else
            task.wait(0.5)
        end
    end
end

-- Auto Stun Loop
task.spawn(function()
    while true do
        local hrp = getRootPart()
        if hrp and characterFolder and remoteEvents then
            for mobName, enabled in pairs(autoStunEnabled) do
                if enabled then
                    for _, mob in ipairs(characterFolder:GetChildren()) do
                        if mob:IsA("Model") and mob.Name:find(mobName) then
                            local part = mob:FindFirstChildWhichIsA("BasePart")
                            if part and (part.Position - hrp.Position).Magnitude <= stunRadius then
                                -- Stun the animal
                                pcall(function()
                                    remoteEvents.StunAnimal:FireServer(mob)
                                end)
                            end
                        end
                    end
                end
            end
        end
        task.wait(0.5)
    end
end)

-- Auto Rescue Child (Updated Targets)
local autoRescueEnabled = false
local CombatTab = Window:CreateTab({Title = "Auto-Rescue", Icon = "user"})
local RescueSection = CombatTab:CreateSection("Auto-Rescue Logic")

RescueSection:CreateToggle({
    Name = "Auto Rescue & Teleport",
    Description = "Finds, captures, and returns to base automatically",
    Callback = function(state)
        autoRescueEnabled = state
        if state then
            task.spawn(function()
                while autoRescueEnabled do
                    local hrp = getRootPart()
                    local campPos = CFrame.new(0, 8, 0) -- Your camp coordinates
                    
                    -- List of all potential targets
                    local targetNames = {
                        "Lost Child", "Lost Child2", "Lost Child3", "Lost Child4",
                        "Dino Kid", "Kraken Kid", "Squid Kid", "Koala Kid"
                    }
                    
                    local foundChild = nil
                    if characterFolder then
                        for _, mob in ipairs(characterFolder:GetChildren()) do
                            if mob:IsA("Model") and table.find(targetNames, mob.Name) then
                                foundChild = mob
                                break
                            end
                        end
                    end

                    if foundChild and hrp then
                        -- 1. TP to Child
                        local targetPart = foundChild:FindFirstChildWhichIsA("BasePart")
                        if targetPart then
                            hrp.CFrame = targetPart.CFrame + Vector3.new(0, 2, 0)
                            task.wait(0.4)
                            
                            -- 2. Capture (Using CaptureChild)
                            pcall(function()
                                if remoteEvents and remoteEvents:FindFirstChild("CaptureChild") then
                                    remoteEvents.CaptureChild:FireServer(foundChild)
                                end
                            end)
                            
                            task.wait(0.5)
                            
                            -- 3. Return to Camp
                            hrp.CFrame = campPos
                        end
                    end
                    task.wait(1.5) -- Wait before checking for the next one
                end
            end)
        end
    end,
    Value = false
})

-- === GAME TP TAB ===
local GameTPTab = Window:CreateTab({Title = "Game TP", Icon = "map"})
local GameTPSection = GameTPTab:CreateSection("Locations")

local storyCoords = {
    ["Campsite"] = "0, 8, -0",
    ["Safe Zone"] = "0, 110, -0"
}

GameTPSection:CreateDropdown({
    Name = "Teleports",
    Description = "Select location",
    Options = {"Campsite", "Safe Zone"},
    Callback = function(selected)
        if storyCoords[selected] then
            teleportToTarget(stringToCFrame(storyCoords[selected]), 0.1)
        end
    end
})

-- === ITEM TP/ESP TAB ===
local ItemTab = Window:CreateTab({Title = "Item TP/ESP", Icon = "package"})
local ESPSection = ItemTab:CreateSection("ESP")
local TPSection = ItemTab:CreateSection("Teleport to Item")
local BringSection = ItemTab:CreateSection("Bring Item to You")

local espConnections = {}

ESPSection:CreateToggle({
    Name = "Item ESP",
    Description = "Show items through walls",
    Callback = function(state)
        local itemFolder = workspace:FindFirstChild("Items")
        if not itemFolder then return end
        
        local itemNames = {
            ["Revolver"] = true, ["Oil Barrel"] = true, ["Chainsaw"] = true, 
            ["Giant Sack"] = true, ["Bunny Foot"] = true,["MedKit"] = true, 
            ["Alien Chest"] = true, ["Berry"] = true, ["Bolt"] = true, 
            ["Broken Fan"] = true, ["Carrot"] = true, ["Coal"] = true,
            ["Coin Stack"] = true, ["Hologram Emitter"] = true, ["Item Chest"] = true,
            ["Laser Fence Blueprint"] = true, ["Log"] = true, ["Old Flashlight"] = true,
            ["Old Radio"] = true, ["Sheet Metal"] = true, ["Bandage"] = true, ["Rifle"] = true
        }
        
        local function createESP(model)
            if not model or not model:IsA("Model") then return end
            if not itemNames[model.Name] then return end
            if not model.PrimaryPart or model:FindFirstChild("ESP") then return end
            
            local billboard = Instance.new("BillboardGui")
            billboard.Name = "ESP"
            billboard.Size = UDim2.new(0, 100, 0, 30)
            billboard.Adornee = model.PrimaryPart
            billboard.AlwaysOnTop = true
            billboard.StudsOffset = Vector3.new(0, 3, 0)
            
            local label = Instance.new("TextLabel")
            label.Size = UDim2.new(1, 0, 1, 0)
            label.BackgroundTransparency = 1
            label.TextColor3 = Color3.new(1, 1, 1)
            label.TextStrokeTransparency = 0.5
            label.Font = Enum.Font.SourceSansBold
            label.Text = model.Name
            label.Parent = billboard
            billboard.Parent = model
        end
        
        if state then
            for _, model in ipairs(itemFolder:GetChildren()) do
                createESP(model)
            end
            local conn = itemFolder.ChildAdded:Connect(function(model)
                if model:IsA("Model") and itemNames[model.Name] then
                    task.wait(0.5)
                    createESP(model)
                end
            end)
            table.insert(espConnections, conn)
        else
            for _, model in ipairs(itemFolder:GetChildren()) do
                local esp = model:FindFirstChild("ESP")
                if esp then esp:Destroy() end
            end
            for _, conn in ipairs(espConnections) do
                if conn then conn:Disconnect() end
            end
            espConnections = {}
        end
    end,
    Value = false
})

local itemNamesList = {
    "Revolver", "Medkit", "Alien Chest", "Berry", "Bolt", "Broken Fan",
    "Carrot", "Coal", "Coin Stack", "Hologram Emitter", "Item Chest",
    "Laser Fence Blueprint", "Log", "Old Flashlight", "Old Radio",
    "Sheet Metal", "Bandage", "Rifle"
}

local function getModelPart(model)
    if not model then return nil end
    if model.PrimaryPart then return model.PrimaryPart end
    for _, part in pairs(model:GetChildren()) do
        if part:IsA("BasePart") then return part end
    end
    return nil
end

TPSection:CreateDropdown({
    Name = "Teleport to Item",
    Description = "Select item to TP to",
    Options = itemNamesList,
    Callback = function(selected)
        local itemFolder = workspace:FindFirstChild("Items")
        if not itemFolder then return end
        
        local candidates = {}
        for _, model in ipairs(itemFolder:GetChildren()) do
            if model:IsA("Model") and model.Name == selected then
                local part = getModelPart(model)
                if part then table.insert(candidates, part) end
            end
        end
        
        if #candidates == 0 then
            warn("No '" .. selected .. "' found!")
            return
        end
        
        local targetPart = candidates[math.random(1, #candidates)]
        local hrp = getRootPart()
        if hrp then hrp.CFrame = targetPart.CFrame + Vector3.new(0, 5, 0) end
    end
})

local possibleItems = {
    "Alien Chest", "Alpha Wolf Pelt", "Anvil Front", "Anvil Back", "Apple",
    "Bandage", "Bear Corpse", "Bear Pelt", "Berry", "Biofuel", "Bolt",
    "Broken Fan", "Bunny Foot", "Carrot", "Coal", "Coin Stack", "Cooked Morsel",
    "Cooked Steak", "Chainsaw", "Cultist", "Cultist Gem", "Flower",
    "Fuel Canister", "Hologram Emitter", "Item Chest", "Laser Fence Blueprint",
    "Leather Body", "Iron Body", "Thorn Body", "Log", "MedKit", "Morsel",
    "Old Flashlight", "Old Radio", "Good Sack", "Good Axe", "Raygun",
    "Giant Sack", "Strong Axe", "Oil Barrel", "Old Car Engine", "Rifle",
    "Rifle Ammo", "Revolver", "Revolver Ammo", "Sapling", "Sheet Metal",
    "Steak", "Wolf Pelt", "Gem of the Forest Fragment", "Tyre",
    "Washing Machine", "Broken Microwave"
}

BringSection:CreateDropdown({
    Name = "Bring Item (Bulk)",
    Description = "Select item to bring",
    Options = possibleItems,
    Callback = function(selected)
        local hrp = getRootPart()
        if not hrp then return end
        local itemsFolder = workspace:FindFirstChild("Items")
        if not itemsFolder or not remoteEvents then return end
        
        local stackOffsetY = 2
        local count = 0
        
        local sources = {itemsFolder}
        local tempStorage = ReplicatedStorage:FindFirstChild("TempStorage")
        if tempStorage then table.insert(sources, tempStorage) end
        
        for _, source in ipairs(sources) do
            for _, item in ipairs(source:GetChildren()) do
                if item.Name == selected then
                    local targetPart = nil
                    if selected == "Berry" then
                        targetPart = item:FindFirstChild("Handle")
                    end
                    if not targetPart then
                        for _, child in ipairs(item:GetDescendants()) do
                            if child:IsA("MeshPart") or child:IsA("Part") or child:IsA("UnionOperation") then
                                targetPart = child
                                break
                            end
                        end
                    end
                    
                    if targetPart then
                        pcall(function()
                            remoteEvents.RequestStartDraggingItem:FireServer(item)
                            local offset = Vector3.new(0, count * stackOffsetY, 0)
                            targetPart.CFrame = hrp.CFrame + offset
                            task.wait(0.05)
                            remoteEvents.StopDraggingItem:FireServer(item)
                        end)
                        count = count + 1
                    end
                end
            end
        end
    end
})

-- === CATEGORIZED ITEM TELEPORTS TAB ===
local CatItemTab = Window:CreateTab({Title = "Item Categories", Icon = "grid"})
local WeaponSection = CatItemTab:CreateSection("Weapons")
local FoodSection = CatItemTab:CreateSection("Food & Consumables")
local MaterialSection = CatItemTab:CreateSection("Materials & Resources")
local GearSection = CatItemTab:CreateSection("Gear & Misc")

local function findTeleportablePart(item)
    if not item then return nil end
    for _, descendant in ipairs(item:GetDescendants()) do
        if descendant:IsA("BasePart") then return descendant end
        if descendant:IsA("Model") then
            for _, sub in ipairs(descendant:GetDescendants()) do
                if sub:IsA("BasePart") then return sub end
            end
        end
    end
    return nil
end

local function teleportItemCategory(itemName)
    local hrp = getRootPart()
    if not hrp then return end
    local itemsFolder = workspace:FindFirstChild("Items")
    if not itemsFolder or not remoteEvents then return end
    
    local stackOffsetY = 2
    local count = 0

    for _, item in ipairs(itemsFolder:GetChildren()) do
        if item.Name == itemName then
            local targetPart = findTeleportablePart(item)
            if targetPart then
                pcall(function()
                    remoteEvents.RequestStartDraggingItem:FireServer(item)
                    local offset = Vector3.new(0, count * stackOffsetY, 0)
                    targetPart.CFrame = hrp.CFrame + offset
                    remoteEvents.StopDraggingItem:FireServer(item)
                end)
                count = count + 1
            end
        end
    end
end

WeaponSection:CreateDropdown({
    Name = "Weapons",
    Description = "Bring weapon to you",
    Options = {"Laser Sword", "Raygun", "Kunai", "Katana", "Spear", "Strong Axe", "Good Axe"},
    Callback = function(selected) teleportItemCategory(selected) end
})

local minifoods = {"Apple", "Berry", "Carrot", "Cake"}
local meat = {"Steak", "Cooked Steak", "Cooked Morsel", "Morsel"}
local consumables = {"Bandage", "MedKit"}

for _, item in ipairs(minifoods) do
    FoodSection:CreateButton({
        Name = "Bring: " .. item,
        Description = "Teleport to you",
        Callback = function() teleportItemCategory(item) end
    })
end
for _, item in ipairs(meat) do
    FoodSection:CreateButton({
        Name = "Bring: " .. item,
        Description = "Teleport to you",
        Callback = function() teleportItemCategory(item) end
    })
end
for _, item in ipairs(consumables) do
    FoodSection:CreateButton({
        Name = "Bring: " .. item,
        Description = "Teleport to you",
        Callback = function() teleportItemCategory(item) end
    })
end

local materials = {"Log", "Coal", "Fuel Canister", "UFO Junk", "UFO Component", "Sheet Metal", 
    "Old Car Engine", "Broken Fan", "Old Microwave", "Old Radio", "Bolt", "Tyre", "Washing Machine",
    "Cultist Gem", "Gem of the Forest Fragment", "Broken Microwave", "Biofuel", "Oil Barrel"}
local pelts = {"Alpha Wolf Pelt", "Bear Pelt", "Wolf Pelt", "Bunny Foot"}

for _, item in ipairs(materials) do
    MaterialSection:CreateButton({
        Name = "Bring: " .. item,
        Description = "Teleport to you",
        Callback = function() teleportItemCategory(item) end
    })
end
for _, item in ipairs(pelts) do
    MaterialSection:CreateButton({
        Name = "Bring: " .. item,
        Description = "Teleport to you",
        Callback = function() teleportItemCategory(item) end
    })
end

local armor = {"Leather Body", "Iron Body", "Thorn Body", "Alien Body"}
local guns = {"Rifle", "Revolver", "Raygun", "Tactical Shotgun", "Revolver Ammo", "Rifle Ammo"}
local tools = {"Good Sack", "Old Flashlight", "Giant Sack", "Strong Flashlight", "Chainsaw", 
    "Hologram Emitter", "Laser Fence Blueprint", "Item Chest", "Alien Chest"}

for _, item in ipairs(armor) do
    GearSection:CreateButton({
        Name = "Bring: " .. item,
        Description = "Teleport to you",
        Callback = function() teleportItemCategory(item) end
    })
end
for _, item in ipairs(guns) do
    GearSection:CreateButton({
        Name = "Bring: " .. item,
        Description = "Teleport to you",
        Callback = function() teleportItemCategory(item) end
    })
end
for _, item in ipairs(tools) do
    GearSection:CreateButton({
        Name = "Bring: " .. item,
        Description = "Teleport to you",
        Callback = function() teleportItemCategory(item) end
    })
end

-- === MOB TP TAB ===
local MobTab = Window:CreateTab({Title = "Mob TP", Icon = "target"})
local MobSection = MobTab:CreateSection("Teleport Mobs")

local possibleCharacters = {
    "Alpha Wolf", "Bear", "Lost Child", "Lost Child2", "Lost Child3", 
    "Lost Child4", "Wolf", "Bunny", "Cultist", "Alien"
}

local function getMainPart(model)
    if not model then return nil end
    if model.PrimaryPart then return model.PrimaryPart end
    for _, part in ipairs(model:GetDescendants()) do
        if part:IsA("BasePart") then return part end
    end
    return nil
end

MobSection:CreateDropdown({
    Name = "Teleport Mob to You",
    Description = "Select mob",
    Options = possibleCharacters,
    Callback = function(selected)
        if not characterFolder then return end
        local hrp = getRootPart()
        if not hrp then return end
        
        local stackOffsetY = 3
        local count = 0
        
        for _, model in ipairs(characterFolder:GetChildren()) do
            if model.Name == selected then
                local mainPart = getMainPart(model)
                if mainPart then
                    local targetCFrame = hrp.CFrame + Vector3.new(0, count * stackOffsetY, 0)
                    if model.PrimaryPart then
                        pcall(function() model:SetPrimaryPartCFrame(targetCFrame) end)
                    else
                        mainPart.CFrame = targetCFrame
                    end
                    count = count + 1
                end
            end
        end
    end
})

-- === PLAYER TAB ===
local PlayerTab = Window:CreateTab({Title = "Player", Icon = "user"})
local PlayerSection = PlayerTab:CreateSection("Character Mods")

PlayerSection:CreateSlider({
    Name = "Jump Power",
    Description = "Modify jump height",
    Min = 50,
    Max = 700,
    Callback = function(value)
        local humanoid = getHumanoid()
        if humanoid then humanoid.JumpPower = value end
    end,
    Value = 50
})

_G.HackedWalkSpeed = 16

local function applyWalkSpeed(humanoid)
    if humanoid then
        humanoid.WalkSpeed = _G.HackedWalkSpeed
        humanoid.Changed:Connect(function(property)
            if property == "WalkSpeed" and humanoid.WalkSpeed ~= _G.HackedWalkSpeed then
                humanoid.WalkSpeed = _G.HackedWalkSpeed
            end
        end)
    end
end

PlayerSection:CreateSlider({
    Name = "Walk Speed",
    Description = "Modify walk speed",
    Min = 16,
    Max = 700,
    Callback = function(value)
        _G.HackedWalkSpeed = value
        applyWalkSpeed(getHumanoid())
    end,
    Value = 16
})

PlayerSection:CreateToggle({
    Name = "WalkSpeed Toggle (50)",
    Description = "Set walkspeed to 50",
    Callback = function(state)
        _G.HackedWalkSpeed = state and 50 or 16
        applyWalkSpeed(getHumanoid())
    end,
    Value = false
})

LocalPlayer.CharacterAdded:Connect(function(char)
    local humanoid = char:WaitForChild("Humanoid", 5)
    if humanoid then applyWalkSpeed(humanoid) end
end)

-- === VISUALS TAB ===
local VisualsTab = Window:CreateTab({Title = "Visuals", Icon = "eye"})
local ESPSection2 = VisualsTab:CreateSection("Player ESP")
local FovSection = VisualsTab:CreateSection("FOV")

local espTransparency = 0.4
local teamCheck = true
local BillboardESPs = {}
local ChamsESPs = {}
local ESPConnections = {}
local ESPEnabled = false
local ChamsEnabled = false

local function createBillboardESP(plr)
    if BillboardESPs[plr] or plr == LocalPlayer then return end
    if not plr.Character then return end
    local head = plr.Character:FindFirstChild("Head")
    if not head then return end
    
    local gui = Instance.new("BillboardGui")
    gui.Name = "Billboard_ESP"
    gui.Adornee = head
    gui.Size = UDim2.new(0, 100, 0, 40)
    gui.AlwaysOnTop = true
    gui.StudsOffset = Vector3.new(0, 2, 0)
    
    local label = Instance.new("TextLabel")
    label.Size = UDim2.new(1, 0, 1, 0)
    label.BackgroundTransparency = 1
    label.TextColor3 = Color3.new(1, 1, 1)
    label.TextStrokeTransparency = 0.5
    label.TextScaled = true
    label.Font = Enum.Font.SourceSansBold
    label.Parent = gui
    
    local conn = RunService.RenderStepped:Connect(function()
        if not plr.Character or not plr.Character:FindFirstChild("Humanoid") then
            gui:Destroy()
            if conn then conn:Disconnect() end
            BillboardESPs[plr] = nil
            ESPConnections[plr] = nil
            return
        end
        local hp = math.floor(plr.Character.Humanoid.Health / plr.Character.Humanoid.MaxHealth * 100)
        label.Text = plr.Name .. " | " .. hp .. "%"
    end)
    
    BillboardESPs[plr] = gui
    ESPConnections[plr] = conn
    gui.Parent = head
end

local function createChamsESP(plr)
    if ChamsESPs[plr] or plr == LocalPlayer then return end
    if not plr.Character then return end
    local hrp = plr.Character:FindFirstChild("HumanoidRootPart")
    if not hrp then return end
    
    local folder = Instance.new("Folder")
    folder.Name = "Chams_ESP"
    folder.Parent = CoreGui
    ChamsESPs[plr] = folder
    
    for _, part in pairs(plr.Character:GetChildren()) do
        if part:IsA("BasePart") then
            local box = Instance.new("BoxHandleAdornment")
            box.Name = "Cham_" .. plr.Name
            box.Adornee = part
            box.AlwaysOnTop = true
            box.ZIndex = 10
            box.Size = part.Size
            box.Transparency = espTransparency
            box.Color = BrickColor.new(teamCheck and (plr.TeamColor == LocalPlayer.TeamColor and "Bright green" or "Bright red") or tostring(plr.TeamColor))
            box.Parent = folder
        end
    end
end

local function cleanupBillboardESP()
    for _, gui in pairs(BillboardESPs) do if gui then gui:Destroy() end end
    for _, conn in pairs(ESPConnections) do if conn then conn:Disconnect() end end
    BillboardESPs = {}
    ESPConnections = {}
end

local function cleanupChamsESP()
    for _, folder in pairs(ChamsESPs) do if folder then folder:Destroy() end end
    ChamsESPs = {}
end

ESPSection2:CreateToggle({
    Name = "Player ESP",
    Description = "Show player names/health",
    Callback = function(state)
        ESPEnabled = state
        if not state then
            cleanupBillboardESP()
        else
            for _, plr in pairs(Players:GetPlayers()) do
                if plr ~= LocalPlayer then createBillboardESP(plr) end
            end
        end
    end,
    Value = false
})

ESPSection2:CreateToggle({
    Name = "Chams",
    Description = "Player box ESP",
    Callback = function(state)
        ChamsEnabled = state
        if not state then
            cleanupChamsESP()
        else
            for _, plr in pairs(Players:GetPlayers()) do
                if plr ~= LocalPlayer then createChamsESP(plr) end
            end
        end
    end,
    Value = false
})

for _, plr in pairs(Players:GetPlayers()) do
    if plr ~= LocalPlayer then
        plr.CharacterAdded:Connect(function()
            task.wait(1)
            if ESPEnabled then createBillboardESP(plr) end
            if ChamsEnabled then createChamsESP(plr) end
        end)
    end
end

Players.PlayerAdded:Connect(function(plr)
    plr.CharacterAdded:Connect(function()
        task.wait(1)
        if ESPEnabled then createBillboardESP(plr) end
        if ChamsEnabled then createChamsESP(plr) end
    end)
end)

local FOVCircle = Drawing.new("Circle")
FOVCircle.Visible = false
FOVCircle.Color = Color3.fromRGB(255, 255, 255)
FOVCircle.Transparency = 1
FOVCircle.Thickness = 1
FOVCircle.Filled = false
FOVCircle.ZIndex = 2
local FOVRadius = 100

RunService.RenderStepped:Connect(function()
    if FOVCircle.Visible then
        FOVCircle.Radius = FOVRadius
        FOVCircle.Position = UserInputService:GetMouseLocation()
    end
end)

FovSection:CreateToggle({
    Name = "FOV Circle",
    Description = "Show FOV circle",
    Callback = function(state) FOVCircle.Visible = state end,
    Value = false
})

FovSection:CreateSlider({
    Name = "FOV Radius",
    Description = "Adjust circle size",
    Min = 50,
    Max = 500,
    Callback = function(value) FOVRadius = value end,
    Value = 100
})

-- === AUTO TAB ===
-- === AUTO TAB ===
local AutoTab = Window:CreateTab({Title = "Auto", Icon = "refresh-cw"})
local CampfireSection = AutoTab:CreateSection("Campfire")
local MachineSection = AutoTab:CreateSection("Machine")
local BiofuelSection = AutoTab:CreateSection("Biofuel Processor")
local EatSection = AutoTab:CreateSection("Auto Eat")
local TreeSection = AutoTab:CreateSection("Trees")

local itemsFolder = workspace:WaitForChild("Items", 5)
local remoteConsume = remoteEvents and remoteEvents:WaitForChild("RequestConsumeItem", 2)

local campfireDropPos = Vector3.new(0, 19, 0)
local machineDropPos = Vector3.new(21, 16, -5)

local function moveItemToPos(item, position)
    if not item or not item:IsDescendantOf(workspace) then return end
    if not remoteEvents then return end
    local part = item.PrimaryPart or item:FindFirstChildWhichIsA("BasePart") or item:FindFirstChild("Handle")
    if not part then return end
    if not item.PrimaryPart then pcall(function() item.PrimaryPart = part end) end
    
    pcall(function()
        remoteEvents.RequestStartDraggingItem:FireServer(item)
        task.wait(0.05)
        item:SetPrimaryPartCFrame(CFrame.new(position))
        task.wait(0.05)
        remoteEvents.StopDraggingItem:FireServer(item)
    end)
end

-- Campfire Fuel Dropdown
local campfireFuelItems = {"Log", "Coal", "Fuel Canister", "Oil Barrel", "Biofuel"}
local campfireFuelOptions = {"All", unpack(campfireFuelItems)}
local enabledCampfireFuels = {}

CampfireSection:CreateDropdown({
    Name = "Auto Feed Campfire",
    Description = "Select fuel items (Choose All for everything)",
    Options = campfireFuelOptions,
    Callback = function(selected)
        if selected == "All" then
            for _, item in ipairs(campfireFuelItems) do
                enabledCampfireFuels[item] = true
            end
            print("[Auto] Enabled all campfire fuels")
        else
            enabledCampfireFuels[selected] = not enabledCampfireFuels[selected]
            print("[Auto] " .. selected .. ": " .. (enabledCampfireFuels[selected] and "ON" or "OFF"))
        end
    end
})

-- MANUAL BRING BUTTON for Campfire
CampfireSection:CreateButton({
    Name = "Bring Selected Fuel NOW",
    Description = "Manually bring selected fuel items immediately",
    Callback = function()
        if not itemsFolder then return end
        local count = 0
        for itemName, enabled in pairs(enabledCampfireFuels) do
            if enabled then
                for _, item in ipairs(itemsFolder:GetChildren()) do
                    if item.Name == itemName then
                        moveItemToPos(item, campfireDropPos)
                        count = count + 1
                    end
                end
            end
        end
        print("[Manual] Brought " .. count .. " fuel items to campfire")
    end
})

local alwaysFeedEnabled = false
CampfireSection:CreateToggle({
    Name = "Always Feed (Ignore HP)",
    Description = "Feed campfire even when HP is full",
    Callback = function(state) alwaysFeedEnabled = state end,
    Value = false
})

-- Cook Dropdown
local cookItems = {"Morsel", "Steak"}
local cookOptions = {"All", unpack(cookItems)}
local enabledCookItems = {}

CampfireSection:CreateDropdown({
    Name = "Auto Cook",
    Description = "Select items to cook",
    Options = cookOptions,
    Callback = function(selected)
        if selected == "All" then
            for _, item in ipairs(cookItems) do enabledCookItems[item] = true end
            print("[Auto] Enabled all cook items")
        else
            enabledCookItems[selected] = not enabledCookItems[selected]
            print("[Auto] " .. selected .. ": " .. (enabledCookItems[selected] and "ON" or "OFF"))
        end
    end
})

-- MANUAL BRING BUTTON for Cook
CampfireSection:CreateButton({
    Name = "Bring Selected Food NOW",
    Description = "Manually bring selected cook items immediately",
    Callback = function()
        if not itemsFolder then return end
        local count = 0
        for itemName, enabled in pairs(enabledCookItems) do
            if enabled then
                for _, item in ipairs(itemsFolder:GetChildren()) do
                    if item.Name == itemName then
                        moveItemToPos(item, campfireDropPos)
                        count = count + 1
                    end
                end
            end
        end
        print("[Manual] Brought " .. count .. " cook items to campfire")
    end
})

-- Grind Dropdown
local grindItems = {"UFO Junk", "UFO Component", "Old Car Engine", "Broken Fan", "Old Microwave", "Bolt", "Log", "Cultist Gem", "Sheet Metal", "Old Radio", "Tyre", "Washing Machine", "Cultist Experiment", "Cultist Component", "Gem of the Forest Fragment", "Broken Microwave"}
local grindOptions = {"All", unpack(grindItems)}
local enabledGrindItems = {}

MachineSection:CreateDropdown({
    Name = "Auto Grind",
    Description = "Select items to grind",
    Options = grindOptions,
    Callback = function(selected)
        if selected == "All" then
            for _, item in ipairs(grindItems) do enabledGrindItems[item] = true end
            print("[Auto] Enabled all grind items")
        else
            enabledGrindItems[selected] = not enabledGrindItems[selected]
            print("[Auto] " .. selected .. ": " .. (enabledGrindItems[selected] and "ON" or "OFF"))
        end
    end
})

-- MANUAL BRING BUTTON for Grind
MachineSection:CreateButton({
    Name = "Bring Selected Items NOW",
    Description = "Manually bring selected grind items immediately",
    Callback = function()
        if not itemsFolder then return end
        local count = 0
        for itemName, enabled in pairs(enabledGrindItems) do
            if enabled then
                for _, item in ipairs(itemsFolder:GetChildren()) do
                    if item.Name == itemName then
                        moveItemToPos(item, machineDropPos)
                        count = count + 1
                    end
                end
            end
        end
        print("[Manual] Brought " .. count .. " grind items to machine")
    end
})

-- Biofuel Dropdown
local biofuelItems = {"Carrot", "Cooked Morsel", "Morsel", "Steak", "Cooked Steak", "Log"}
local biofuelOptions = {"All", unpack(biofuelItems)}
local enabledBiofuelItems = {}

BiofuelSection:CreateDropdown({
    Name = "Auto Biofuel",
    Description = "Select items to process",
    Options = biofuelOptions,
    Callback = function(selected)
        if selected == "All" then
            for _, item in ipairs(biofuelItems) do enabledBiofuelItems[item] = true end
            print("[Auto] Enabled all biofuel items")
        else
            enabledBiofuelItems[selected] = not enabledBiofuelItems[selected]
            print("[Auto] " .. selected .. ": " .. (enabledBiofuelItems[selected] and "ON" or "OFF"))
        end
    end
})

-- MANUAL BRING BUTTON for Biofuel
BiofuelSection:CreateButton({
    Name = "Bring Selected Items NOW",
    Description = "Manually bring selected biofuel items immediately",
    Callback = function()
        local hrp = getRootPart()
        if not hrp or not itemsFolder then return end
        
        -- Get biofuel processor position
        local structures = workspace:FindFirstChild("Structures")
        local processor = structures and structures:FindFirstChild("Biofuel Processor")
        local procPart = processor and processor:FindFirstChild("Part")
        if not procPart then 
            print("[Error] Biofuel Processor not found!")
            return 
        end
        local biofuelProcessorPos = procPart.Position + Vector3.new(0, 5, 0)
        
        local count = 0
        for itemName, enabled in pairs(enabledBiofuelItems) do
            if enabled then
                for _, item in ipairs(itemsFolder:GetChildren()) do
                    if item.Name == itemName then
                        moveItemToPos(item, biofuelProcessorPos)
                        count = count + 1
                    end
                end
            end
        end
        print("[Manual] Brought " .. count .. " items to biofuel processor")
    end
})

-- Auto Eat
local autoEatEnabled = false
EatSection:CreateToggle({
    Name = "Auto Eat (3s)",
    Description = "Eat every 3 seconds",
    Callback = function(state) autoEatEnabled = state end,
    Value = false
})

local autoEatHPEnabled = false
EatSection:CreateToggle({
    Name = "Auto Eat (Hunger < 50%)",
    Description = "Eat when hunger drops below 50%",
    Callback = function(state) autoEatHPEnabled = state end,
    Value = false
})

-- MANUAL EAT BUTTON
EatSection:CreateButton({
    Name = "Eat Food NOW",
    Description = "Manually eat available food immediately",
    Callback = function()
        if not itemsFolder or not remoteConsume then return end
        local available = {}
        for _, item in ipairs(itemsFolder:GetChildren()) do
            if table.find({"Cooked Steak", "Cooked Morsel", "Berry", "Carrot", "Apple"}, item.Name) then
                table.insert(available, item)
            end
        end
        if #available > 0 then
            local food = available[math.random(1, #available)]
            pcall(function() remoteConsume:InvokeServer(food) end)
            print("[Manual] Ate: " .. food.Name)
        else
            print("[Manual] No food available!")
        end
    end
})

-- Auto Trees
local originalTreeCFrames = {}
local treesBrought = false

local function getAllSmallTrees()
    local trees = {}
    local map = workspace:FindFirstChild("Map")
    if not map then return trees end
    local foliage = map:FindFirstChild("Foliage")
    local landmarks = map:FindFirstChild("Landmarks")
    
    local function scan(folder)
        if not folder then return end
        for _, obj in ipairs(folder:GetChildren()) do
            if obj:IsA("Model") and obj.Name == "Small Tree" then
                table.insert(trees, obj)
            end
        end
    end
    scan(foliage)
    scan(landmarks)
    return trees
end

local function findTrunk(tree)
    if not tree then return nil end
    for _, part in ipairs(tree:GetDescendants()) do
        if part:IsA("BasePart") and part.Name == "Trunk" then return part end
    end
    return nil
end

TreeSection:CreateToggle({
    Name = "Auto Bring All Small Trees",
    Description = "Bring trees to you (loops continuously)",
    Callback = function(state)
        if state and not treesBrought then
            local hrp = getRootPart()
            if not hrp then return end
            local target = CFrame.new(hrp.Position + hrp.CFrame.LookVector * 10)
            for _, tree in ipairs(getAllSmallTrees()) do
                local trunk = findTrunk(tree)
                if trunk then
                    if not originalTreeCFrames[tree] then originalTreeCFrames[tree] = trunk.CFrame end
                    tree.PrimaryPart = trunk
                    trunk.Anchored = false
                    trunk.CanCollide = false
                    task.wait()
                    pcall(function() tree:SetPrimaryPartCFrame(target + Vector3.new(math.random(-5,5), 0, math.random(-5,5))) end)
                    trunk.Anchored = true
                end
            end
            treesBrought = true
            print("[Auto] Brought all small trees to you")
        elseif not state and treesBrought then
            for tree, cframe in pairs(originalTreeCFrames) do
                if tree and tree.Parent then
                    local trunk = findTrunk(tree)
                    if trunk then
                        tree.PrimaryPart = trunk
                        pcall(function() tree:SetPrimaryPartCFrame(cframe) end)
                        trunk.Anchored = true
                        trunk.CanCollide = true
                    end
                end
            end
            originalTreeCFrames = {}
            treesBrought = false
            print("[Auto] Restored all trees to original positions")
        end
    end,
    Value = false
})

-- MANUAL BRING TREES BUTTON
TreeSection:CreateButton({
    Name = "Bring Trees NOW",
    Description = "Manually bring all small trees immediately",
    Callback = function()
        local hrp = getRootPart()
        if not hrp then return end
        local target = CFrame.new(hrp.Position + hrp.CFrame.LookVector * 10)
        local count = 0
        for _, tree in ipairs(getAllSmallTrees()) do
            local trunk = findTrunk(tree)
            if trunk then
                if not originalTreeCFrames[tree] then originalTreeCFrames[tree] = trunk.CFrame end
                tree.PrimaryPart = trunk
                trunk.Anchored = false
                trunk.CanCollide = false
                pcall(function() tree:SetPrimaryPartCFrame(target + Vector3.new(math.random(-5,5), 0, math.random(-5,5))) end)
                trunk.Anchored = true
                count = count + 1
            end
        end
        treesBrought = true
        print("[Manual] Brought " .. count .. " trees to you")
    end
})

-- Auto Loops (Background looping - continuously looks for items)
coroutine.wrap(function()
    while true do
        if alwaysFeedEnabled and itemsFolder then
            for itemName, enabled in pairs(enabledCampfireFuels) do
                if enabled then
                    for _, item in ipairs(itemsFolder:GetChildren()) do
                        if item.Name == itemName then moveItemToPos(item, campfireDropPos) end
                    end
                end
            end
        end
        task.wait(2)
    end
end)()

coroutine.wrap(function()
    local map = workspace:WaitForChild("Map", 5)
    local campfire = map and map:FindFirstChild("Campground") and map.Campground:FindFirstChild("MainFire")
    local fillFrame = campfire and campfire:FindFirstChild("Center") and campfire.Center:FindFirstChild("BillboardGui") and campfire.Center.BillboardGui:FindFirstChild("Frame") and campfire.Center.BillboardGui.Frame:FindFirstChild("Background") and campfire.Center.BillboardGui.Frame.Background:FindFirstChild("Fill")
    
    while true do
        if not alwaysFeedEnabled and fillFrame and itemsFolder then
            local healthPercent = fillFrame.Size.X.Scale
            if healthPercent < 0.7 then
                repeat
                    for itemName, enabled in pairs(enabledCampfireFuels) do
                        if enabled then
                            for _, item in ipairs(itemsFolder:GetChildren()) do
                                if item.Name == itemName then moveItemToPos(item, campfireDropPos) end
                            end
                        end
                    end
                    task.wait(0.5)
                    healthPercent = fillFrame.Size.X.Scale
                until healthPercent >= 1
            end
        end
        task.wait(2)
    end
end)()

coroutine.wrap(function()
    while true do
        if itemsFolder then
            for itemName, enabled in pairs(enabledCookItems) do
                if enabled then
                    for _, item in ipairs(itemsFolder:GetChildren()) do
                        if item.Name == itemName then moveItemToPos(item, campfireDropPos) end
                    end
                end
            end
        end
        task.wait(2.5)
    end
end)()

coroutine.wrap(function()
    while true do
        if itemsFolder then
            for itemName, enabled in pairs(enabledGrindItems) do
                if enabled then
                    for _, item in ipairs(itemsFolder:GetChildren()) do
                        if item.Name == itemName then moveItemToPos(item, machineDropPos) end
                    end
                end
            end
        end
        task.wait(2.5)
    end
end)()

coroutine.wrap(function()
    local biofuelProcessorPos
    while true do
        if not biofuelProcessorPos then
            local structures = workspace:FindFirstChild("Structures")
            local processor = structures and structures:FindFirstChild("Biofuel Processor")
            local part = processor and processor:FindFirstChild("Part")
            if part then biofuelProcessorPos = part.Position + Vector3.new(0, 5, 0) end
        end

        if biofuelProcessorPos and itemsFolder then
            for itemName, enabled in pairs(enabledBiofuelItems) do
                if enabled then
                    for _, item in ipairs(itemsFolder:GetChildren()) do
                        if item.Name == itemName then moveItemToPos(item, biofuelProcessorPos) end
                    end
                end
            end
        end
        task.wait(2)
    end
end)()

coroutine.wrap(function()
    while true do
        if autoEatEnabled and itemsFolder and remoteConsume then
            local available = {}
            for _, item in ipairs(itemsFolder:GetChildren()) do
                if table.find({"Cooked Steak", "Cooked Morsel", "Berry", "Carrot", "Apple"}, item.Name) then
                    table.insert(available, item)
                end
            end
            if #available > 0 then
                local food = available[math.random(1, #available)]
                pcall(function() remoteConsume:InvokeServer(food) end)
            end
        end
        task.wait(3)
    end
end)()

coroutine.wrap(function()
    local playerGui = LocalPlayer:WaitForChild("PlayerGui", 5)
    local interface = playerGui and playerGui:FindFirstChild("Interface")
    local statBars = interface and interface:FindFirstChild("StatBars")
    local hungerBar = statBars and statBars:FindFirstChild("HungerBar") and statBars.HungerBar:FindFirstChild("Bar")
    
    while true do
        if autoEatHPEnabled and hungerBar and itemsFolder and remoteConsume then
            if hungerBar.Size.X.Scale <= 0.5 then
                repeat
                    local available = {}
                    for _, item in ipairs(itemsFolder:GetChildren()) do
                        if table.find({"Cooked Steak", "Cooked Morsel", "Berry", "Carrot", "Apple"}, item.Name) then
                            table.insert(available, item)
                        end
                    end
                    if #available > 0 then
                        local food = available[math.random(1, #available)]
                        pcall(function() remoteConsume:InvokeServer(food) end)
                    else break end
                    task.wait(1)
                until (hungerBar.Size.X.Scale >= 0.99)
            end
        end
        task.wait(3)
    end
end)()

-- === MISC TAB ===
local MiscTab = Window:CreateTab({Title = "Misc", Icon = "settings"})
local MiscSection = MiscTab:CreateSection("Extra Scripts")

MiscSection:CreateButton({
    Name = "Infinite Yield",
    Description = "Load Infinite Yield",
    Callback = function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/EdgeIY/infiniteyield/master/source"))()
    end
})

MiscSection:CreateButton({
    Name = "Emote GUI",
    Description = "Load R6 Animations",
    Callback = function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/dimension-sources/random-scripts-i-found/refs/heads/main/r6%20animations"))()
    end
})

MiscSection:CreateButton({
    Name = "Anti AFK",
    Description = "Prevent kicking for AFK",
    Callback = function()
        local vu = game:GetService("VirtualUser")
        game:GetService("Players").LocalPlayer.Idled:Connect(function()
            vu:CaptureController()
            vu:ClickButton2(Vector2.new())
        end)
        print("Anti-AFK enabled")
    end
})

MiscSection:CreateButton({
    Name = "Turtle Spy",
    Description = "Load Turtle Spy",
    Callback = function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/Turtle-Brand/Turtle-Spy/main/source.lua", true))()
    end
})

print("99 Nights in the Forest - WindUI Loaded (Complete + Anti-Log + Godmode + Combat)!")